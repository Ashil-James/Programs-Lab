DATA SEGMENT
    DIVIDEND DW 1000h
    DIVISOR  DW 000Ah
    QUOT DW ?
    REM  DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX, DATA
    MOV DS, AX
    
    MOV AX, DIVIDEND
    DIV DIVISOR
    
    MOV QUOT, AX
    MOV REM, DX
    
    MOV AH, 4Ch
    INT 21h
CODE ENDS
END START 